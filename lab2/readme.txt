To compile the source:

Get object files for each source using the command:
	gcc filename.c -c -o filename.o
	
To link all needed object files, use the command:
	gcc file1.o file2.o file3.o (and so on...)	-o progname
